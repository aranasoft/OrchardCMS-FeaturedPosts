@using Orchard.ContentManagement
@{
    ContentItem contentItem = Model.Content.ContentItem.BlogPostMenuItemPart.Content;
}

@if (contentItem != null) {
    <a href="@Model.Href">@Model.Text</a> 
}